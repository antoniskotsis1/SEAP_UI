# Base path for the real SEAPP backend (server.servlet.context-path).
# Defaults to "/seappi" when unset. The typed clients in src/lib/services.ts
# (producersApi / fieldsApi / productionsApi) hit this base.
VITE_API_BASE_URL=/seappi
